
server:
  port: 2020

spring:
  application:
    name: greenhouse_system
  resources:
    static-locations: classpath:/
  # 数据源
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root123
    url: jdbc:mysql://127.0.0.1:3306/greenhouse_system?useSSL=FALSE&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Shanghai


   # jdbc:mysql://127.0.0.1:3306/greenhouse_system?characterEncoding=utf8&useSSL=false


# mybaits 模块配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.douzi.greenhouse_system.entity

